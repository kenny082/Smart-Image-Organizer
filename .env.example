# API Security
SECRET_KEY=your-secret-key-here  # Generate using: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-api-key-here        # Generate using: python -c "import secrets; print(secrets.token_urlsafe(16))"

# Rate Limiting
MAX_REQUESTS_PER_MINUTE=60

# Caching
CACHE_SIZE=1000

# Server Settings
HOST=0.0.0.0
PORT=8000
